---
title: reset()
description: BProgress.reset() method
---

The `reset()` method is used to reset the progress bar to the default state.

## Usage

```js
BProgress.reset();
```

## Typing

```ts
BProgress.reset(): BProgress;
```
